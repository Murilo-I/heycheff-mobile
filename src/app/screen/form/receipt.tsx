import { styles } from "@/styles/global";
import { Text, View } from "react-native";

export default function ReceiptForm() {
    return (
        <View style={styles.flexCenter}>
            <Text style={styles.fontRegular}>Formulário</Text>
        </View>
    );
}